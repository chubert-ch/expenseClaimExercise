services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: expense_claim
      POSTGRES_USER: expense
      POSTGRES_PASSWORD: expense
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
