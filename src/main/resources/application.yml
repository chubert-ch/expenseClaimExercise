spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/expense_claim
    username: expense
    password: expense
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true

server:
  port: 8080

app:
  cors:
    allowed-origins: http://localhost:3000
